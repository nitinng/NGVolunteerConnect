# Code Standards

All code must follow the project's linting and formatting standards.

Rules:

- Use TypeScript strictly. No implicit `any`.
- Use functional React components only.
- Use arrow functions for all components.
- Follow Prettier formatting.
- Use named exports unless a framework explicitly requires default exports.
