# UI Component Rules

All UI components must use shadcn/ui components whenever available.

Rules:

- Use shadcn primitives before creating custom UI components.
- Use Tailwind CSS for styling.
- Do not write raw CSS unless absolutely necessary.
- Components must support both light mode and dark mode.
- Use shadcn blocks for common layouts such as cards, forms, tables, and dashboards.
