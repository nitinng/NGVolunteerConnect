# Design System Rules

Follow the global design tokens defined in styles.md.

Rules:

- Use design tokens instead of hardcoded colors.
- Do not hardcode hex colors.
- Always use theme variables.
- Use the spacing scale defined in Tailwind.
- Use consistent radius values from CSS variables.
- All components must use `rounded-lg`.
- Follow the typography scale defined in the design system.
