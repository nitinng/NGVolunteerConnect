# Accessibility Rules

All UI must meet accessibility standards.

Rules:

- All interactive elements must have labels.
- Use semantic HTML wherever possible.
- Forms must include validation messages.
- Ensure keyboard navigation works correctly.
